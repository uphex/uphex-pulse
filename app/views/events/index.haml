- content_for :title do
  -if defined? @client
    %a{:href =>  "/clients/#{@client.id}"}
      %span.icon-arrow-left2
  - else
    %a{:href =>  "/"}
      %span.icon-arrow-left2
  #{(defined? @client) ? @client.name : "All events"}
.all-events
  = partial 'layouts/timeline' , :locals=>{:timelineevents=>@allevents, :eventurl=>"events/", :eventurlback=>"", :hidden=>"true"}
  %a{:class => "more-events button-standard", :id => 'see-all'} See more